[workspace]
name = "can_commsgen"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.11"

[pypi-dependencies]
can_commsgen = { path = ".", editable = true }
pytest = "*"
ruff = "*"
pyright = "*"

[tasks]
pytest = "pytest"
ruff = "ruff"
pyright = "pyright"
